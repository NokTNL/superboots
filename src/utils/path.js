import path from "path";

// `path.dirname` returns the directory of a file is located in
// `require.main.filename` is the full path of the file where the process started (!!! Not available in ES modules)
// export const rootDir = path.dirname(require.main?.filename);
